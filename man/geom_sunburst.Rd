% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_sunburst.R
\name{geom_sunburst}
\alias{geom_sunburst}
\title{Sunburst Geom for ggplot2}
\usage{
geom_sunburst(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  na.rm = FALSE,
  border_color = "white",
  border_width = 0.8,
  show.legend = NA,
  inherit.aes = TRUE,
  ...
)
}
\arguments{
\item{mapping}{Aesthetic mapping created by \code{aes()}.}

\item{data}{The data to be displayed (from \code{prepare_sunburst_data}).}

\item{stat}{The statistical transformation to use.}

\item{position}{Position adjustment.}

\item{na.rm}{Remove missing values?}

\item{border_color}{Color of segment borders (default "white").}

\item{border_width}{Width of segment borders (default 0.8).}

\item{show.legend}{Include in legend?}

\item{inherit.aes}{Inherit aesthetics from ggplot call?}

\item{...}{Other arguments passed to layer.}
}
\value{
A ggplot2 layer.
}
\description{
A geom for drawing sunburst chart segments. This is primarily used
internally by \code{sunburst()} but can be used directly for custom plots.
}
\examples{
library(ggplot2)

# Create example data
df <- data.frame(
  visit = rep(c("V1", "V2", "V3"), each = 100),
  grade = sample(0:4, 300, replace = TRUE)
)
plot_data <- prepare_sunburst_data(df, "visit", "grade")

ggplot(plot_data) +
  geom_sunburst() +
  coord_polar(theta = "x") +
  theme_void()
}
